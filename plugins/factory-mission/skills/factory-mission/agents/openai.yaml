interface:
  display_name: "Factory Mission"
  short_description: "Plan and operate Factory Missions safely"
  default_prompt: "Use $factory-mission to prepare or operate a Factory Mission."
policy:
  allow_implicit_invocation: true
